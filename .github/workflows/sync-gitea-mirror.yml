name: Sync Gitea Mirror (Legacy Push Path)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain current mirror mode
        run: |
          echo "Daily mirror now runs as an internal pull job from the cluster side."
          echo "Use ops-runbooks/scripts/daily-github-to-gitea-sync.ps1 on internal infrastructure."
          echo "This GitHub-hosted push path is intentionally disabled for private Forge access."
